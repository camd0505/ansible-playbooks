- name: find files older than 180 days
  find:
    path: '{{ /data/HOSTS }}'
	age:"179d"
    recurse: yes
    file_type: file
  register: oldfiles

- name: Remove files
  file:
    path: '{{ /data/HOSTS }}'
    state: absent
  with_items:
    - '{{ oldfiles.files }}'
